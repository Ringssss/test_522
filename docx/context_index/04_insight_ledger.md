# Insight Ledger

## Purpose

This is the canonical living ledger for dLLM / MoE / EB optimization insights in this project. It is meant to guide direction selection before implementation.

Use this file to answer:

- Which mechanisms are strongly supported by experiments?
- Which mechanisms were weakened or rejected?
- Which optimization directions follow from the evidence?
- What evidence should be checked before investing in native or Triton implementation?

Process documents remain the source of detailed history. This ledger is the compact maintained index.

## Method Notes

- Monkey-patch implementations are valid for fast mechanism validation.
- Monkey-patch performance should not be treated as the final achievable performance when Python/control overhead dominates.
- A mechanism should only be promoted to native/Triton/vLLM integration when the observed benefit source is large enough to survive implementation overhead.
- For MoE optimizations, distinguish theoretical FLOP/expert-pair savings from wall-clock savings. Current fused MoE kernels are often constrained by expert weight HBM loading.

## Insight Table

| ID | Insight | Mechanism | Data Source | Status | Optimization Implication |
|---:|---|---|---|---|---|
| I1 | Forward count is the highest-leverage dLLM variable. | dLLM decodes a block through repeated full model forwards; removing one forward saves attention, MoE, LMHead, cache work, and distributed communication together. | KC 2026-04-09 to 2026-04-11; BD+IterSmooth experiments | Strongly supported | Decoder, threshold, block iteration, and confidence policy should stay first-class optimization targets. |
| I2 | Cache is not monotonically beneficial in dLLM. | Batch=1 cache paths suffer from cache management and low H100 utilization; large-batch long-context no-cache paths suffer from `O(batch * seq^2)` attention scaling. | KC 2026-04-10 to 2026-04-11 | Strongly supported | Runtime should select cache/no-cache by batch size and context length instead of using one global policy. |
| I3 | MASK routing concentration is a dLLM-specific MoE phenomenon. | MASK tokens share embeddings and produce similar hidden states, especially in shallow layers, concentrating expert choices and causing load imbalance. | KC 2026-04-12; P11 E1-E5 validation | Strongly supported | Use load-aware expert placement, block-start load analysis, and EPLB-style balancing. |
| I4 | Cross-iteration MoE redundancy exists, but direct output reuse is unsafe. | Decoded/stable positions show routing/output similarity, but MoE-output approximation errors propagate through attention and layers. | KC 2026-04-12; v0.1.13 ablations | Mechanism supported; direct skipping rejected | Avoid full-output stable cache. If revisiting, use risk guards, low-precision recompute, or limited/local approximations. |
| I5 | `S_mask` has strong within-block temporal stability. | Cold path constructs an expert budget, and hot/skip paths can reuse it over repeated block forwards; `q_major=1.0` improves stability. | P8 S_mask stability; P11 Insight A; P12g path counts | Strongly supported | Reuse EB metadata and schedule decisions around block boundaries. |
| I6 | `S_mask` stability does not imply hidden/output stability. | `S_mask` only constrains the expert candidate set; hidden states, expert weights, and attention-conditioned outputs can still drift. | Stable cache failures; P12g Scheme3 analysis | Strongly supported | Reuse metadata/schedules, not routed MoE outputs without strong guards. |
| I7 | EB value grows with batch size. | Larger heterogeneous batches activate more unique experts; limiting the candidate expert set reduces active expert count more effectively. | P8 HetEval-128 M-sweep | Supported | EB is more promising for large-batch serving than small-batch single-request optimization. |
| I8 | Fused MoE wall time is often dominated by expert weight HBM loading. | Weight-zero and token-expert-pair pruning do not help unless they reduce unique active experts and weight loads. | P8n NCU; KC 2026-04-14 kernel micro-benchmark | Strongly supported | Optimize active expert count / expert weight loading, not just FLOPs or per-token weights. |
| I9 | Padding-free MoE is not a promising primary direction here. | Padding compute is not the bottleneck; vLLM fused MoE kernels are already H100-tuned and memory-bound. | KC 2026-04-13; P8n | Rejected as primary path | Do not spend more effort on padding-free kernels unless workload changes. |
| I10 | `topk=4` is a real but quality-sensitive signal. | Reducing selected experts from 8 to 4 preserved forward count in key runs and reduced ms/fwd at batch=128, but quality showed mild degradation in at least one verifiable prompt. | P8n routing topk compression | Promising, needs broader quality validation | Worth validating as a native MoE/topk path with larger quality suite. |
| I11 | Top-p expert pruning theoretical savings do not directly imply wall-clock savings. | Top-p reduces token-expert FLOPs, but unique active expert count can remain almost unchanged, so weight loading remains. | KC 2026-04-14; kernel micro-benchmark | Revised downward | Top-p needs active-expert compaction or native kernel support to become wall-clock useful. |
| I12 | Python monkey-patch is a good mechanism-validation tool, not a final performance form. | It enables fast A/B testing, but Python and per-layer control overhead often eats small gains. | v0.1.14 hook overhead; P12g Scheme3 | Methodological insight | Use monkey-patch for feasibility; only native/Triton-integrate mechanisms with large enough benefit sources. |
| I13 | Scheme3 routing-logits communication saving is real but too small as a standalone target. | Dispatch payload falls from about 827 MB/fwd to about 706 MB/fwd, but wall-clock dispatch improves only about 0.45-0.52 ms/fwd. | P12g 8-GPU timing | Mechanism supported; priority lowered | Do not focus on routing-logits communication alone. Combine only if larger structural changes also exist. |
| I14 | dLLM + DP AllToAll has a collective-alignment problem. | Different DP ranks can converge in different numbers of block iterations, while AllToAll collectives require identical call order. | P9 DP AllToAll adaptation | Strongly supported | Multi-GPU dLLM needs block/iteration-level synchronization and scheduler-aware design. |
| I15 | TP/parallelism structure can dominate local MoE patch gains. | dInfer originally duplicated attention under TP-like multi-GPU execution; C11 attention TP and LMHead TP reduced major redundancy. | P10; P11 C11/C12 | Strongly supported | Sequence Parallel / TP / LMHead TP should remain high-priority system directions. |
| I16 | Expert popularity is data-dependent, not a model constant. | Hot expert/GPU patterns differ by dataset; static replication or fixed placement assumptions fail. | P11 Insight C | Strongly supported | Prefer online/adaptive placement or load-aware routing over fixed model-level expert assumptions. |
| I17 | Block boundary is a natural synchronization and planning point. | Shape, block id, cold EB state, cache reset/update, and dLLM iteration state align at block starts. | P11 Insight E; P12 Scheme3 block clock | Architectural insight | Use block starts for safe scheduling, graph capture boundaries, EB cold planning, and synchronization. |
| I18 | Shared expert is important but cannot replace routed experts. | Shared expert contribution is large, but shared-only approximation is not accurate enough; routed expert path remains essential. | KC 2026-04-12; v0.1.14 | Supported | Treat shared expert as a guard or signal, not a replacement for routed MoE. |
| I19 | BSP-MoE validates TP-local MoE token de-duplication, but collective overhead is now the blocker. | Splitting `N=local_bsz*seq_len` across TP ranks reduces dispatch payload from `826.877` to `206.719 MB/fwd`; no-timing e2e improves only `1.40%`, while component timing shows AgRs combine and TP all-gather overhead. | P12j BSP-MoE validation | Mechanism supported; native implementation needed | Keep BSP as a high-value system direction, but next work must optimize collective layout/native integration rather than add Python monkey-patches. |
| I20 | Each MoE layer has independent experts; cross-layer load "rotation" does not reduce per-layer synchronization cost. | Layer N's 256 experts are completely separate weight matrices from Layer M's. Inference is sequential (layer-by-layer with collective sync barriers). The busiest GPU in each layer determines that layer's wall-clock time. Overall ep_load_cv (cross-layer average) is misleading; per-layer per-GPU metrics are what matter. | P15.15 heatmap profiling; C12 lb2 data | Strongly supported | Always evaluate EPLB benefit using per-layer metrics (layer_ep_load_cv, layer_ep_load_max_mean), not overall ep_load_cv. |
| I21 | Per-layer expert load is extremely skewed (CV~1.2, top10% handle ~37% tokens, max/min ratio ~65000x), but hot experts differ completely across layers. | Per-layer per-expert heatmap shows no expert appears in all 19 layers' top-5 (83 unique experts across 95 slots). This confirms routing concentration (I3) but shows it targets different experts in each layer. | P15.15 routing heatmap c12_hm11 | Strongly supported | EPLB placement should be per-layer (vllm's hierarchical algorithm already does this). A single global expert replication strategy is suboptimal. |
| I22 | Per-layer token imbalance (max/mean=1.22~1.41) causes only 0.3~0.6 ms wall-clock improvement when perfectly balanced — experimentally confirmed memory-bound dampening. | Clean A/B with zero runtime overhead: OLD linear expert_map (per-layer GPU CV=0.14~0.20) vs NEW vllm per-layer balanced expert_map (CV≈0.0000). GS: 76.99→76.71 ms (-0.28 ms, -0.36%). A: 81.28→80.67 ms (-0.61 ms, -0.75%). The 22~41% token imbalance translates to only 0.4~0.8% wall-clock difference because fused MoE is memory-bound (I8). | P15.15 per-layer balanced A/B; heatmap c12_hm11; vllm_perlayer_balanced_expert_map_ep8_20260502.pt | **Experimentally confirmed** | EPLB runtime map overhead (+2.2%) far exceeds the achievable per-layer balance benefit (~0.5%). Static per-layer balanced placement (vllm balanced_packing + real load data) is the correct zero-cost approach. Runtime EPLB is only justified when: (a) kernel becomes compute-bound, (b) EP scale >> 8 GPUs, (c) batch << 512. |
| I23 | Per-layer balanced expert placement using vllm balanced_packing + real load profiling data is a free lunch. | Using per-layer per-expert heatmap data as input to vllm's balanced_packing produces a per-layer expert_map where each layer's hot experts are spread across GPUs (19 unique layer patterns, per-layer GPU CV≈0). This requires zero runtime overhead — only a one-time offline profiling run + map generation. Wall-clock gain is small (~0.5%) but cost is zero. | P15.15 per-layer balanced A/B; vllm DefaultEplbPolicy.balanced_packing | Strongly supported | Adopt as default expert_map generation pipeline: (1) profile one C12 run to get per-layer per-expert loads, (2) feed to balanced_packing, (3) save as expert_map. Redo when workload distribution changes significantly. |
| I25 | kernel_time = f(total_pairs_per_GPU), independent of how pairs are distributed across experts. Per-layer Pearson r = +0.93 confirms timing gap is load-driven, not noise. Dampening = 0.06x at batch=512 (memory-bound). | Active Expert Sweep: fixed total_pairs=8192 with active_experts=[4,8,16,32] shows kernel time constant (~1.15 ms/layer). Total Pairs Sweep: fixed 32 active experts with pairs=[2048→32768] shows sublinear growth. 2D Sweep (6×4=24 points) confirms: low pairs → active experts matter 10-23%, high pairs → <6%. Per-layer causal analysis: 19 layers × 8 GPUs = 152 data points, mean per-layer Pearson r = +0.928, tok_CV=0.286, qa_CV=0.017, dampening=0.06x. | P15.16 active expert sweep, total pairs sweep, 2D sweep, causal analysis | **Experimentally confirmed** | EPLB (which only changes distribution, not total pairs) cannot improve kernel time in principle. The 0.06x dampening means even large load imbalance translates to negligible timing differences at our batch=512 memory-bound operating point. |
| I26 | EPLB (redundant experts) is a net negative optimization at EP=8. It increases quant_apply spread (1.26→7.24 ms at batch=2048), and combine "overhead" is 100% waiting time. | batch=2048 compute-bound A/B: EPLB OFF qa_spread=1.26 ms, EPLB ON qa_spread=7.24 ms (+5.98 ms). Combine rank-min decreases (-0.81 ms = communication unchanged), combine rank-max increases (+5.51 ms = pure waiting). Per-forward actual imbalance cost: Σ qa_gap = 0.93 ms (batch=512, 1.2% of 77 ms), 3.14 ms (batch=2048, 1.6% of 200 ms). | P15.16 EPLB ON/OFF at batch=2048; per-layer per-GPU timing | **Experimentally confirmed** | Do not use EPLB redundant experts at EP=8 single node. The mechanism worsens imbalance by making non-bottleneck GPUs faster (via fewer experts) while bottleneck GPU stays the same, increasing the wait gap. |
| I27 | E=32/34/36 FusedMoE tiling configs are exact copies of E=256, not independently auto-tuned. E=62/128/512 have significantly different optimal configs. | Byte-for-byte comparison of config JSON files. E=32 config = E=256 config (100% identical). E=62 differs: e.g. at M=256, E=32 uses BSN=256,BSK=128,warps=8 vs E=62 uses BSN=32,BSK=64,warps=4. Config selection: `E, _, N = w2_shape` → loads `E={E},N={N},device_name=xxx.json` → selects by closest M. | P15.16 tiling config analysis; vllm fused_moe.py source | Strongly supported | Auto-tune E=32,N=512 config independently. The grid is EM tiles × N tiles, so different E values (affecting EM through moe_align_block_size padding) should have different optimal tiling. |
| I28 | E=32 FusedMoE tiling config at actual inference M=16384 is already near-optimal. Grid search over 384 configs yields only 2-3% micro-bench improvement; no measurable e2e gain. | AgRs AllGather inflates M from local 2048 to 16384 (8x), so config selection hits the large-M region where E=256's optimal config (copied to E=32) is already correct. Tiling auto-tune, pre-filter block waste removal (-1.8% regression), and small tile config (M_eff=256, -2.8% regression) all excluded. | P15.17 tiling autotune; autotune_fused_moe_e32.py sweep | **Experimentally confirmed** | Further tiling optimization for E=32 at batch=512 is not worthwhile. The M=16384 operating point is in the large-M regime where tiling config has minimal sensitivity. |
| I29 | AgRs dispatch+combine = 21.7 ms/fwd, decomposed as: NCCL communication 7.8 ms (36%) + straggler waiting 4.5 ms (21%) + framework/Python overhead 8.7 ms (42%). | Sync+barrier insertion isolates combine straggler (12.7→8.2 ms, 4.5 ms transfers to barrier). NCCL micro-bench: single AG 9MB = 0.249 ms (253 GB/s). fp8 half payload 3.2x faster. Framework overhead is distributed across entire forward chain, not concentrated in MoE. | P15.17 dispatch/combine analysis; nccl_comm_bench.py; nccl_overhead_isolation.py | Strongly supported | Framework overhead (8.7 ms, 42%) is the largest dispatch+combine component. EPLB can only affect ~2 ms (straggler kernel component). fp8 communication can save ~5 ms (NCCL portion). CUDA Graph targets framework overhead but only saves ~2 ms at batch=512 (I30). |
| I30 | CUDA Graph at batch=512 saves only ~2 ms (2%) because GPU pipeline hides Python dispatch overhead. At batch=64, CUDA Graph saves 30% (59.5→41.5 ms). Benefit is inversely proportional to batch size. | MoE-only graph: 2.43→2.24 ms/layer (0.93 ms total). Full forward graph (batch=64, no KV cache): 59.5→41.5 ms (30%). Full forward (batch=512, KV cache): SDPA regression > graph savings. EB+graph: only 2 ms saved. torch.compile: inductor incompatible with EP .cpu() sync. | P15.18 CUDA Graph experiments 1-5; poc_cudagraph_*.py | **Experimentally confirmed** | CUDA Graph direction is excluded at batch=512. The framework overhead (I29, 8.7 ms) exists but is hidden by GPU pipeline at large batch. Only relevant for small batch (<64) or latency-sensitive scenarios. |
| I31 | GS path GPU utilization is only 71%. The 29% idle time comes from kernel launch gaps (16 ms/fwd of >50us gaps). All operations execute on a single CUDA stream with zero NCCL-compute overlap. | nsys profiling: kernel launch gap median 1.7us, P99 187us. Large gaps (>50us): 16 ms/fwd total, distributed as before-NCCL 6.8ms, after-GEMM 5.2ms, after-NCCL 3.9ms. Cross-rank compute CV <1% (perfectly balanced). Cross-rank NCCL variance 16% (2.8ms, topology). DPMetadata AllReduce: 1.7 ms/fwd pure sync overhead. | P15.18 nsys analysis; /home/wuhang/tmp_nsys/profile_gs_full.nsys-rep | Strongly supported | Multi-stream NCCL-compute overlap is a theoretical optimization direction but shared expert overlap (OPT-5) showed ~0 ms gain — GPU pipeline already hides the latency at batch=512. Single-stream execution is not a performance bottleneck at current batch size. |
| I32 | LM head ([16384,2048]×[2048,157184]) + logits.float() = 11.5 ms/fwd (15% of baseline), the largest non-MoE cost item. SP LM Head optimization saves 7.7 ms (11.7%): skip last-layer TP all-gather, compute lm_head on SP layout [N_sp, vocab], local decode, all-gather only decoded token IDs (128KB). | Component timing: lm_head=7.4 ms, logits.float=4.1 ms. OPT-2 (skip logits.float): -4.3 ms verified. SP LM Head: G path 65.9→58.2 ms (-7.7 ms). Math equivalence: sigmoid is monotonic → argmax(sigmoid(logits))=argmax(logits). Each SP rank has full vocab dimension; softmax/argmax/threshold computed locally is exact. Quality verified via heteval512 visual inspection. | P15.18 experiments 6,8,11; modeling_llada2_moe.py SP-LM changes; parallel_strategy.py local decode | **Experimentally confirmed** | SP LM Head is the highest-value optimization found (7.7 ms, 11.7%). Combined with OPT-2: baseline 70.2→58.2 ms/fwd (1.21x). Requires env vars DINF_SKIP_LOGITS_FLOAT=1 + DINF_SP_LM_HEAD=1. |
| I33 | fp8 dispatch causes -1.5 ms regression (NVLink bandwidth sufficient, cast overhead > communication savings). Shared expert ∥ dispatch overlap yields ~0 ms gain (GPU pipeline already hides shared expert latency). Both excluded at batch=512 + NVLink. | fp8 dispatch: G path 65.9→67.4 ms (+1.5 ms). NVLink bandwidth 250 GB/s, fp8 cast kernel overhead exceeds halved payload savings. Shared expert overlap: two-stream implementation, ~0 ms within noise. GPU pipeline at batch=512 already hides 3.4 ms shared expert compute during 8.7 ms dispatch. | P15.18 experiments 9-10 | **Experimentally confirmed** | Communication compression and compute-communication overlap have negligible benefit at batch=512 + NVLink high-bandwidth configuration. These directions are only relevant for cross-node EP (lower bandwidth) or smaller batch (exposed pipeline gaps). |

## Status Legend

| Status | Meaning |
|---|---|
| Strongly supported | Repeatedly validated by experiments or source-level constraints. |
| Supported | Evidence is positive, but scope or quality validation should still be expanded. |
| Promising, needs broader quality validation | Performance signal exists; correctness/quality risk remains. |
| Mechanism supported; direct skipping rejected | The observed phenomenon is real, but the naive optimization path failed. |
| Revised downward | The mechanism remains true but estimated wall-clock value was reduced by later evidence. |
| Rejected as primary path | Do not prioritize unless workload or implementation assumptions change. |
| Methodological insight | Guidance about how to run experiments, not a model/runtime mechanism itself. |
| Architectural insight | Stable structural property that guides system design. |

## Source Index

| Short Name | Source |
|---|---|
| KC | `/home/wuhang/wuhang/dllm_wh/code_building/key_conclusion.md` |
| P8 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.8-eb_hot_path_optimization_and_batch_scaling.md` |
| P8n | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.8n-ncu_profiling_tiling_and_multi_gpu.md` |
| P9 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.9-dp_alltoall_ep_adaptation.md` |
| P10 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.10-deepep_integration_and_attn_tp.md` |
| P11 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.11-c11_c12_tp_attention_and_insight_optimization.md` |
| P12g | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.12g-scheme3_8g_timing_and_quality.md` |
| P12j | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.12j-bsp_moe_validation.md` |
| P15.15 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.15-eb_eplb_source_landing_execution.md` |
| P15.16 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.16-eplb_causal_chain_and_kernel_microbench.md` |
| P15.17 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.17-tiling_autotune_and_dispatch_combine_analysis.md` |
| P15.18 | `/home/wuhang/wuhang/dllm_wh/code_building/process_docs/v0.1-init-project/v0.1.15.18-cudagraph_profiling_and_sp_lm_head.md` |

## Candidate Direction Buckets

| Direction | Primary Insights | Current Judgment |
|---|---|---|
| Reduce forward count | I1, I2 | Highest leverage; should remain a primary line. |
| Sequence/TP/parallelism structure | I14, I15, I17, I19 | High-value system direction after C11/C12; BSP validates the mechanism but shifts the bottleneck to native collective layout. |
| Native active-expert reduction | I5, I7, I8, I10, I11 | Promising only if it reduces unique active experts or weight loading. |
| Block-stage-aware serving scheduler | I2, I3, I14, I16, I17 | Promising for production serving; needs multi-request validation. |
| Scheme3-style routing-logits communication | I5, I13 | Mechanism is valid but standalone priority is low. |
| EPLB runtime load balancing | I3, I8, I20, I21, I22, I23, I25, I26 | Per-layer token imbalance is real (CV=0.14~0.20, max/mean=1.22~1.41) but experimentally confirmed to yield only ~0.5% wall-clock gain when perfectly balanced (memory-bound dampening 0.06x). Runtime map overhead (+2.2%) far exceeds this. EPLB redundant experts are net negative (increase spread). **Adopt static per-layer balanced placement (I23) as default; runtime EPLB only for larger EP scale or compute-bound kernels.** |
| FusedMoE tiling config optimization | I8, I27, I28 | E=32,N=512 config is copied from E=256 without auto-tuning (I27), but at actual inference M=16384 the config is already near-optimal (I28). **Excluded at batch=512; only relevant if dispatch changes reduce M.** |
| CUDA Graph / framework overhead | I29, I30, I31 | Dispatch+combine framework overhead is 8.7 ms/42% (I29), but GPU pipeline hides it at batch=512 → CUDA Graph saves only 2 ms (I30). GPU util 71% from kernel launch gaps (I31), but single-stream execution is not fixable without multi-stream which also showed ~0 ms gain. **Excluded at batch=512.** |
| LM head / output path optimization | I32 | LM head+logits.float = 11.5 ms/15% (I32). SP LM Head saves 7.7 ms (1.21x cumulative). **Already implemented and verified; further gains require vocab pruning or kernel fusion.** |
| Communication compression / overlap | I29, I33 | fp8 dispatch regresses (-1.5 ms) at NVLink bandwidth (I33). Shared expert overlap ~0 ms (I33). NCCL portion of dispatch is 7.8 ms but NVLink already saturates. **Excluded at single-node NVLink; relevant for cross-node EP.** |

## Update Rules

When adding or changing an insight:

1. Add the source document or result file to the Source Index if needed.
2. Update the Status field rather than deleting old conclusions.
3. If an optimization path is rejected, preserve the mechanism if it remains true.
4. Append a short update to `/home/wuhang/wuhang/dllm_wh/code_building/progress_diff_summary.md`.
5. If the change affects top-level direction, add a short note to `/home/wuhang/wuhang/dllm_wh/code_building/key_conclusion.md`.
